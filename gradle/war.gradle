apply plugin: "war"

bootWar {
    mainClassName = "ua.goit.JhipsterSampleApplicationApp"
    includes = ["WEB-INF/**", "META-INF/**"]
}

war {
    enabled = true
    archiveExtension = "war.original"
    includes = ["WEB-INF/**", "META-INF/**"]

}
